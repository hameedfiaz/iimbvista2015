<div class="container-fluid filter">
	<div class="row-fluid">
		<div class="col-md-12">
			<center>
				<div class="filter-options btn-group">
					<button <%= "data-group='all'" %> class="btn btn--warning filter_btn">All</button>
					<% @event_categories.each do |event_category| %>
						<button <%= "data-group='#{event_category.id}'" %> class="btn btn--warning filter_btn"><%= event_category.title %></button>
					<% end %>
					<button <%= "data-group='flagship'" %> class="btn btn--warning filter_btn">Flagship</button>
				</div>
			</center>
		</div>
	</div>
	<div class="row-fluid">
		<div class="col-md-9"></div>
		<div class="col-md-3">
			<div class="row-fluid">
				<input type="search" placeholder="Search..." class="filter__search js-shuffle-search">
			</div>
		</div>
	</div>

</div>

<div id="grid" class="row-fluid">
		<% @events.each do |event| %>
			<% if event %>
				<div class="item blue col-md-3" <%= "data-title='#{event.title}'" %> <%= event.flagship ? "data-groups='[\"all\",\"flagship\",\"#{event.event_category_id}\"]'" : "data-groups='[\"all\",\"#{event.event_category_id}\"]'"%>>
					<span class="picture-item__title"><a <%= "href='/myevents/information/#{event.id}'" %>><%= event.title %></a></span>
				</div>
			<% end %>
		<% end %>
</div>

